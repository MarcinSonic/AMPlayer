<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    android:layout_height="match_parent"
    android:layout_width="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
<LinearLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    tools:context="pl.marcingorski.amplayer.MainActivity">
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="260dp">

        <ImageView
            android:id="@+id/title_background"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/girl"
            android:alpha=".80"
            android:scaleType="centerCrop"
            />
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:orientation="vertical">
        <TextView
            android:id="@+id/title"
            android:textStyle="bold"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:text="@string/app_name"
            android:textColor="#FFFFFF"
            android:textSize="52sp" />

        <TextView
            android:id="@+id/subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textAlignment="center"
            android:text="@string/start_listening_sub_title"
            android:textColor="#FFFFFF"
            android:textSize="32sp" />
        </LinearLayout>

    </RelativeLayout>

    <TextView
        android:id="@+id/music"
        style="@style/CategoryStyle"
        android:textAlignment="center"
        android:background="@color/category_music"
        android:text="@string/category_music" />

    <TextView
        android:id="@+id/audiobooks"
                style="@style/CategoryStyle"
        android:textAlignment="center"
        android:background="@color/category_audiobooks"
        android:text="@string/category_audiobooks"
        />
</LinearLayout>
</ScrollView>